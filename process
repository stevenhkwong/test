-- linke this repo to the remote one:
git remote add origin git@github.com:stevenhkwong/test.git

-- switch to dev branch (or start)
git checkout -b dev

-- start the dev branch on the rmote
push origin dev:dev

-- add some changes etc 
..
..
git commit
git push

----- now merge back to master (associate push with local master to origin master)
git checkout master
git merge dev
git push origin master:master


-- switch back to dev branch to develop
git checkout dev
